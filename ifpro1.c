#include <stdio.h>

void main()
{
    int number,ans;
    printf("enter the value of number");
    scanf("%d",&number);
    if(number>=4)
    {
        ans=number*number;
        printf("enter the ans %d",ans);
    }

    
}